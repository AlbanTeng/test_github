# Make sure "Git Enabled" has been checked
    #Click Settings -> enter "git enabled"
# Git Add remote URL and repo name
    # Click View -> enter Git Add Remote
    # Enter Git repo url via login github website and create a repo that is as same name as local repo(/project)
# in Source control side bar(third one in common), in Message box, enter init to initalize the one.
    # Click Push button to upload local data on github

#modify local data and then upload the change
    # Click Source control side bar, in the Message box, enter "name change"(which means commit the changes)
    # Click push button to upload the change on github  

#for more details, please check the video https://www.youtube.com/watch?v=3Tn58KQvWtU
